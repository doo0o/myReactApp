{
  "dependencies": {
    "gh-pages": "^4.0.0",
    "gulp": "^4.0.2"
  },
  "homepage": "https://doo0o.github.io/myReactApp/index",
  "deploy": "gh-pages -d build",
  "predeploy": "npm run build",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build"
  }
}
